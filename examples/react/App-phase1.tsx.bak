import React from 'react';
import { useMarketplaceSession } from './useMarketplaceSession';

export function App() {
  const {
    session,
    loading,
    error,
    formattedTimeWithHours,
    isTimerRunning,
    pauseTimer,
    resumeTimer,
    endSession,
  } = useMarketplaceSession({
    jwksUri: 'http://localhost:8080/.well-known/jwks.json',
    applicationId: 'app_test_123',
    debug: true,
    autoStart: true,
    warningThresholdSeconds: 300,
  });

  if (loading) {
    return (
      <div style={styles.container}>
        <div style={styles.card}>
          <div style={styles.spinner} />
          <p>Initializing session...</p>
        </div>
      </div>
    );
  }

  if (error) {
    return (
      <div style={styles.container}>
        <div style={{ ...styles.card, ...styles.errorCard }}>
          <h2>❌ Error</h2>
          <p>{error}</p>
        </div>
      </div>
    );
  }

  if (!session) {
    return null;
  }

  return (
    <div style={styles.container}>
      <div style={styles.card}>
        <h1 style={styles.title}>🎟️ Session Dashboard</h1>
        <p style={styles.subtitle}>Marketplace Provider SDK - React Example</p>

        <div style={styles.timerCard}>
          <div style={styles.timerLabel}>Time Remaining</div>
          <div style={styles.timerValue}>{formattedTimeWithHours}</div>
        </div>

        <div style={styles.infoGrid}>
          <div style={styles.infoItem}>
            <div style={styles.infoLabel}>Session ID</div>
            <div style={styles.infoValue}>{session.sessionId}</div>
          </div>
          <div style={styles.infoItem}>
            <div style={styles.infoLabel}>User ID</div>
            <div style={styles.infoValue}>{session.userId}</div>
          </div>
          <div style={styles.infoItem}>
            <div style={styles.infoLabel}>Organization</div>
            <div style={styles.infoValue}>{session.orgId}</div>
          </div>
          <div style={styles.infoItem}>
            <div style={styles.infoLabel}>Duration</div>
            <div style={styles.infoValue}>{session.durationMinutes} minutes</div>
          </div>
        </div>

        <div style={styles.buttonGroup}>
          {isTimerRunning ? (
            <button style={{ ...styles.button, ...styles.secondaryButton }} onClick={pauseTimer}>
              ⏸ Pause
            </button>
          ) : (
            <button style={{ ...styles.button, ...styles.primaryButton }} onClick={resumeTimer}>
              ▶️ Resume
            </button>
          )}
          <button
            style={{ ...styles.button, ...styles.dangerButton }}
            onClick={() => {
              if (window.confirm('Are you sure you want to end this session?')) {
                endSession();
              }
            }}
          >
            🛑 End Session
          </button>
        </div>
      </div>
    </div>
  );
}

const styles: Record<string, React.CSSProperties> = {
  container: {
    minHeight: '100vh',
    background: 'linear-gradient(135deg, #667eea 0%, #764ba2 100%)',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    padding: '20px',
    fontFamily: '-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif',
  },
  card: {
    background: 'white',
    borderRadius: '16px',
    boxShadow: '0 20px 60px rgba(0, 0, 0, 0.3)',
    padding: '40px',
    maxWidth: '600px',
    width: '100%',
  },
  title: {
    color: '#333',
    marginBottom: '10px',
    fontSize: '28px',
  },
  subtitle: {
    color: '#666',
    marginBottom: '30px',
  },
  timerCard: {
    background: '#f8f9fa',
    borderRadius: '8px',
    padding: '20px',
    marginBottom: '20px',
  },
  timerLabel: {
    fontSize: '12px',
    textTransform: 'uppercase',
    letterSpacing: '1px',
    color: '#888',
    marginBottom: '8px',
  },
  timerValue: {
    fontSize: '32px',
    fontWeight: '600',
    color: '#667eea',
  },
  infoGrid: {
    display: 'grid',
    gridTemplateColumns: '1fr 1fr',
    gap: '15px',
    marginBottom: '20px',
  },
  infoItem: {
    background: '#f8f9fa',
    borderRadius: '8px',
    padding: '15px',
  },
  infoLabel: {
    fontSize: '11px',
    textTransform: 'uppercase',
    letterSpacing: '1px',
    color: '#888',
    marginBottom: '5px',
  },
  infoValue: {
    fontSize: '14px',
    fontWeight: '500',
    color: '#333',
    wordBreak: 'break-all',
  },
  buttonGroup: {
    display: 'flex',
    gap: '10px',
  },
  button: {
    flex: 1,
    padding: '12px 24px',
    border: 'none',
    borderRadius: '8px',
    fontSize: '14px',
    fontWeight: '600',
    cursor: 'pointer',
    transition: 'all 0.2s',
  },
  primaryButton: {
    background: '#667eea',
    color: 'white',
  },
  secondaryButton: {
    background: '#e0e0e0',
    color: '#333',
  },
  dangerButton: {
    background: '#dc3545',
    color: 'white',
  },
  errorCard: {
    background: '#fee',
    border: '1px solid #fcc',
  },
  spinner: {
    border: '3px solid #f3f3f3',
    borderTop: '3px solid #667eea',
    borderRadius: '50%',
    width: '40px',
    height: '40px',
    animation: 'spin 1s linear infinite',
    margin: '0 auto 20px',
  },
};
